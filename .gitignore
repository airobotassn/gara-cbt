# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local.txt

# Playwright (검증 산출물)
/tests/shots/
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# 개인 커리어 기록 (회사 repo에 커밋하지 않음)
# ⚠️ career-log 스킬이 쓰는 건 한글 파일이지만, 영문 이름 파일도 따로 굴러다닌다.
#    git 입장에선 완전히 다른 경로라 위 한 줄로는 안 걸린다 → 둘 다 적는다.
docs/커리어기록.md
docs/커리어기록-*.md
docs/career-log.md

# 작업 부산물 (git 에 넣지 않음 — 합쳐서 265MB, 한 번 들어가면 히스토리에 영구히 남는다)
# scratchpad = 스크린샷·_dev.log / artifacts = 시안 PNG·HTML / .gjc = 세션 캐시
scratchpad/
artifacts/
.gjc/

# 루트에 굴러다니는 1회용 작업 스크립트 (에셋 자르기·화면 재보기·소켓 찔러보기 등).
# 지우지는 않는다 — 쓰던 걸 계속 쓰되 git 에만 안 올린다.
/_*.mjs
/pick-play6.png

# Supabase CLI 가 잘못된 위치(supabase/functions/)에서 실행됐을 때 만드는 찌꺼기.
# linked-project.json 에 프로젝트 ref 가 들어있다.
supabase/functions/supabase/

# 이 기기에서만 쓰는 설정 (MCP 서버 활성화 목록 등)
.claude/settings.local.json

# 리다이렉트 사고로 생긴 빈 파일들 (전부 0바이트). NUL 은 윈도 예약어.
(1)
(2)
(3)
(4)
A
NUL
Negative
Number

# 작업 중 생기는 디버그 산출물 (스샷·목빌드·임시 스크립트) — 저장소에 올리지 않는다
.codex-*.png
_shots/
_mockbuild/
_mchk.cjs


# 프로덕션 데이터 덤프 — 개인정보 포함, 절대 커밋 금지
backup/
