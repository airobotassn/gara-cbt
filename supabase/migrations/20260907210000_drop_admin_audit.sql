-- 2026-09-07 · 관리자 감사로그(admin_audit) 제거 — 쓰기만 있고 보는 화면이 0곳 (2026-09-07 지시)
--
--   "누가 언제 뭘 했나" 를 남기려고 만든 표다. 되돌릴 수 없는 조작(회원 온보딩 초기화·탈퇴 복구·
--   문항 삭제·정책 변경 등)에서 16곳이 이 표에 썼고, 53건이 쌓여 있었다.
--
--   ⛔ **그런데 아무도 못 본다.** 서버에 목록을 내려주는 액션(`auditList`)까지 만들어져 있는데
--      그걸 부르는 화면이 **0곳**이다. 관리자 화면 어디에도 감사로그 메뉴가 없다.
--      쓰기만 있고 읽기가 없는 로그는 없는 것과 같다 — 사고가 나도 아무도 열어보지 못한다.
--
--   ⚠️ **잃는 것이 있다는 건 분명히 해둔다.** 이제 "누가 남의 계정을 초기화했나" 에 답할 수단이 없다.
--      되돌리기 전 값(`before`)은 여전히 응답으로 돌아가니 **누른 사람 화면에만** 남는다.
--      ⛔ 되살릴 거면 **보는 화면을 같이** 만들 것. 화면 없이 표만 되살리면 같은 상태로 돌아간다.
--
--   ⛔ **함수 배포가 끝난 뒤에 적용할 것** (admin).
--      아직 도는 옛 코드가 이 표에 insert 한다 — 먼저 지우면 PostgREST 가 실패한다.
--      다만 insert 는 전부 try/catch 로 삼키거나(reform 의 audit 헬퍼) 이미 끝난 조작 뒤라서
--      운영이 멈추지는 않는다. 그래도 순서는 지킨다.
--
--   지우기 전: 53행 (actor_email 2명 · action 16종 · 2026-08-11 ~)

begin;

drop table if exists admin_audit;

commit;
