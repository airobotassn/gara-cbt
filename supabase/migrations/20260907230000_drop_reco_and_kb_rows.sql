-- 2026-09-07 · 옛 레벨 추천 제거 + 지식베이스 조각 비우기 (2026-09-07 지시)
--
--   ① 레벨 추천 (표 2 · RPC 1 · 엣지 함수 1) — 통째로 제거
--      "검색어 → Gemini 임베딩 → 가장 비슷한 옛 문구를 찾아 그 레벨을 추천" 이었다.
--      랜딩 검색이 **의미 라우터**(route-query → 페이지 이동)로 바뀌면서 대체됐고,
--      `recommend-level` 을 부르는 화면이 **0곳**이 됐다(프론트가 부르는 함수 36개 목록에 없다).
--      `reco_shadow_log` 는 "캐시만 써도 되나" 를 확인하려고 LLM 판정과 캐시 답을 나란히 적던 표인데
--      **마지막 기록이 2026-06-17** 이다(사흘 쓰고 껐다).
--      ⚠️ `/test/select` 는 추천 레벨을 랜딩에서 **넘겨받기만** 한다(navState.recommendedLevel) —
--         만들던 쪽이 없어지는 것이고 받는 쪽 코드는 그대로다(값이 없으면 내 등급이 기본이 된다).
--      ⛔ 되살릴 거면 표를 복구하지 말고 **지금 라우터 위에서 다시 설계할 것** — 앵커 문구는
--         이미 `route_cache` 에 767개 있고, 그때의 237개는 옛 설계의 캐시라 그대로는 못 쓴다.
--
--   ② kb_chunks — **행만 비운다. 표와 기능은 남긴다.**
--      ⛔ 표를 지우면 관리자 문항 생성 화면이 깨진다(자료 넣기 → 문항 생성 → 발행 → 임베딩 백필
--         파이프라인이 kb-save·kb-generate·kb-embed-backfill 로 이 표를 쓴다). 지금 안 쓸 뿐이지
--         기능은 살아 있다 — "할 때 다시 넣는다"(2026-09-07 지시)는 데이터를 다시 모은다는 뜻이다.
--      비우는 이유 둘:
--        · 2,546개 중 **임베딩이 734개(29%)뿐**이라 나머지는 검색에 안 걸린다. 어차피 다시 떠야 한다.
--        · 축 코드가 어긋나 있다 — Lv.6 자리에 `l5_*` 축 6종, Lv.7 자리 것이 Lv.6 에 `l6_*` 로 남아 있다.
--          2026-07 사다리 밀기 때 `level` 만 밀고 `axis` 를 안 민 자리다. 지금 비우면 그 빚도 같이 사라진다.
--      ⚠️ 백업에 그대로 있다 — `backup/20260904/kb_chunks.json`(2,546행 · 8.9MB).
--
--   ⛔ **함수 배포가 끝난 뒤에 적용할 것.** 옛 recommend-level 배포본이 살아 있는 동안 이 표를
--      지우면 그 함수가 500 을 낸다(부르는 화면은 없지만 배포본은 남아 있다).
--      배포본 삭제 + 마이그레이션을 같은 묶음으로 처리한다.

begin;

-- ① 레벨 추천
drop function if exists public.match_reco_cache(vector, int);
drop table if exists reco_cache;
drop table if exists reco_shadow_log;

-- ② 지식베이스 조각 — 표는 남기고 내용만 비운다.
truncate table kb_chunks;

-- 검산 — kb_chunks 표는 살아 있어야 한다(관리자 문항 생성 화면이 쓴다).
do $$
begin
  if to_regclass('public.kb_chunks') is null then
    raise exception 'kb_chunks 표가 사라졌다 — 관리자 문항 생성 화면이 깨진다';
  end if;
end $$;

commit;
