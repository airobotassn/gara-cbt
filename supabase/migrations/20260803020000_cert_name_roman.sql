-- ============================================================
-- 자격증 영문 성명(cert_name_roman) — 새 자격증 시안이 영문 서식이라 이름을 로마자로 각인한다.
--   · 왜 profiles 가 아니라 응시 기록에 두는가: 자격증은 **발급 시점의 스냅샷**이어야 한다.
--     나중에 프로필을 고쳐도 이미 발급된 증서(재발급 포함)의 이름은 그대로여야 위조·불일치 시비가 없다.
--   · 입력 시점 = 발급 신청(결제) 화면. my-attempts 의 issue 액션이 같이 받아 저장한다.
--   · 여권 표기와 맞추는 값이라 자동 로마자 변환을 하지 않는다(성씨 표기가 사람마다 다름: 이 = Lee/Yi/Rhee).
--   멱등(재실행 안전).
-- ============================================================
alter table exam_attempts add column if not exists cert_name_roman text;
