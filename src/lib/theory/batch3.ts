// DAILY QUIZ 해설 — 배치 3 · 학습과 그 한계.
// 키 = 정답 용어(한국어). 그림 키는 components/dailyVisuals/batch3.tsx 의 VISUALS_B3 와 짝이다.
//
// 이 아홉 개 중 다섯(머신러닝·파인튜닝·RAG·사전 학습·RLHF)은 "무엇을 고치는가" 축으로 갈린다 —
// 규칙 대신 경계를 찾고(머신러닝), 모델 내부를 고치고(파인튜닝), 모델은 두고 자료만 갈아 끼우고(RAG),
// 그 내부를 처음 다지고(사전 학습), 사람의 선택을 점수판 삼아 다시 고친다(RLHF).
// 나머지 넷(환각·편향·적대적 공격·휴먼 인 더 루프)은 그 학습이 새는 지점이다.
import type { TermTheory } from './types'

export const THEORY_B3: Record<string, TermTheory> = {
  머신러닝: {
    point: '규칙을 사람이 적지 않는다. 예시에서 경계를 스스로 찾는다.',
    visual: 'b3_ml',
    compare: '규칙은 사람이 하나씩 늘리고, 학습은 예시만 더 주면 된다.',
  },
  파인튜닝: {
    point: '이미 학습된 모델의 내부 숫자판을 도메인 데이터로 다시 조정한다.',
    visual: 'b3_finetune',
    compare: 'RAG 는 모델 밖 문서를 갈아 끼우고, 파인튜닝은 모델 안을 고친다.',
  },
  RAG: {
    point: '모델은 그대로 두고, 답하기 전에 문서를 찾아와 근거로 붙인다.',
    visual: 'b3_rag',
    compare: '파인튜닝은 모델을 고치고, RAG 는 옆에 문서를 갈아 끼운다.',
  },
  환각: {
    point: '근거가 없어도 확률만 높으면 문장은 끝까지 만들어진다.',
    visual: 'b3_hallucination',
    compare: '편향은 데이터가 한쪽으로 쏠린 문제, 환각은 근거 없이 지어내는 문제.',
  },
  편향: {
    point: '데이터가 한쪽에 쏠리면 판정선도 그쪽으로 기운다.',
    visual: 'b3_bias',
    compare: '환각은 근거가 없는 문제, 편향은 데이터가 치우친 문제.',
  },
  RLHF: {
    point: '사람이 더 나은 답을 고르면, 그 선택이 점수가 돼 모델을 다시 조율한다.',
    visual: 'b3_rlhf',
    compare: '파인튜닝은 정답 데이터로, RLHF 는 사람의 선호로 모델을 고친다.',
  },
  '적대적 공격': {
    point: '사람 눈에 안 보이는 잡음이 모델의 판정만 뒤집는다.',
    visual: 'b3_adversarial',
    compare: '환각은 근거 없이 지어내는 것, 적대적 공격은 일부러 속이는 것.',
  },
  '사전 학습': {
    point: '정답표 없이 방대한 글을 읽으며 언어의 틀을 처음 잡는 단계.',
    visual: 'b3_pretrain',
    compare: '사전 학습은 처음 다지는 단계, 파인튜닝·RLHF 는 그 뒤를 다듬는 단계.',
  },
  '휴먼 인 더 루프': {
    point: '자동 처리 결과를 사람이 확인해야 다음 단계로 넘어가는 구조.',
    visual: 'b3_hitl',
    compare: '게이트가 있으면 틀린 값이 걸리고, 없으면 그대로 나간다.',
  },
}
