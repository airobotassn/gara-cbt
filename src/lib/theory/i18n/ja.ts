// DAILY QUIZ 해설 번역 — ja. 이 파일은 **묶기만** 한다(실제 문구는 아래 두 파일).
// ⚠️ 라벨(그림 속 글자)과 해설 글을 파일로 나눈 이유: 한 파일에 500개를 넣으면 사람이 못 고친다.
//    화면에서는 한 벌로 쓰이므로 여기서 합친다.
import type { DailyI18nPack } from '../../dailyI18n'
import { LABELS } from './ja.labels'
import { THEORY } from './ja.theory'

export const PACK: DailyI18nPack = { labels: LABELS, theory: THEORY }
