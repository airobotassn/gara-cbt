// 러닝 라이브러리 왼쪽 열의 색 점 — 컴포넌트가 아니라 값이라 **별도 파일**이다.
//   (컴포넌트 파일에서 같이 export 하면 react-refresh 가 "이 파일은 컴포넌트만 내보내야 한다"로 막는다.)

/** 레벨 칸의 색 점 — **그 레벨 교재 표지의 네온색**에서 뽑은 값이다(표지 그림에서 실측).
 *  ⚠️ testConfigLevel 의 LEVEL_COLORS(연두→빨강)를 쓰지 않는다. 이 화면은 바로 옆에 표지가 서 있어서
 *     사다리색을 쓰면 점과 표지가 서로 다른 색을 말한다. 표지를 새로 뽑으면 이 값도 다시 재야 한다. */
export const COVER_COLORS: Record<number, string> = {
  1: '#2edef9', // 시안
  2: '#0632f1', // 파랑
  3: '#755ef4', // 보라
  4: '#8d49f7', // 자주
  5: '#f52c8f', // 핑크
  6: '#f35907', // 주황
  7: '#da1919', // 빨강
}

/** 레벨/급수 사다리 밖이라는 뜻으로 무채색(slate-400). '레벨 무관'·'급수 무관' 칸이 쓴다. */
export const ANY_COLOR = 'rgb(148 163 184)'
